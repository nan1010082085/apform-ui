# ContentPanel

白底内容面板（title / extra / flush / fill）。

## API

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `title` | `string` | — | 标题 |
| `flush` | `boolean` | `false` | 去掉 body 内边距 |
| `fill` | `boolean` | `false` | flex 撑满 |
