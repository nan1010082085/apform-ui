/**
 * ErrorBoundary 文档元数据
 */
import type { ComponentDoc } from '../../docs/types'

/** ErrorBoundary 文档 */
export const ErrorBoundaryDoc: ComponentDoc = {
  name: 'ErrorBoundary',
  description: 'ErrorBoundary 组件。',
  props: [
  ],
  emits: [
  ],
  slots: [
  ],
}
